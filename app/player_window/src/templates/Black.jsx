import React from 'react';

const Template = (props) => (
    <div style={Styles.container}>
    </div>
)

const Styles = {
    container: {
        width: '100%',
        height: '100%',
        background: '#000000',
    },
}

export default Template